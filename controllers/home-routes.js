//================[Dependencies]====================/
const router = require("express").Router();
const sequelize = require("../config/connection");

//================[GET ROUTES]====================/
router.get('/', (req, res) => {

});

module.exports = router;
